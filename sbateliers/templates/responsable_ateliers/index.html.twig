{% extends 'base.html.twig' %}

{% block title %}ResponsableAteliers index{% endblock %}

{% block body %}
    <h1>ResponsableAteliers index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for responsable_atelier in responsable_ateliers %}
            <tr>
                <td>{{ responsable_atelier.id }}</td>
                <td>{{ responsable_atelier.nom }}</td>
                <td>{{ responsable_atelier.prenom }}</td>
                <td>
                    <a href="{{ path('app_responsable_ateliers_show', {'id': responsable_atelier.id}) }}">show</a>
                    <a href="{{ path('app_responsable_ateliers_edit', {'id': responsable_atelier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_responsable_ateliers_new') }}">Create new</a>
{% endblock %}
