{% extends 'base.html.twig' %}

{% block title %}ResponsableAteliers{% endblock %}

{% block body %}
    <h1>ResponsableAteliers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responsable_atelier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ responsable_atelier.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ responsable_atelier.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_responsable_ateliers_index') }}">back to list</a>

    <a href="{{ path('app_responsable_ateliers_edit', {'id': responsable_atelier.id}) }}">edit</a>

    {{ include('responsable_ateliers/_delete_form.html.twig') }}
{% endblock %}
